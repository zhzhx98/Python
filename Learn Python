#2020.6.9

刪除空格
  name.rstrip
  name.lstrip
  name.strip

输出为字符
  str()

-1返回列表最后一个元素

添加表格
  name.append()

插入新元素到表格
  name.insert(位置,元素)

删除表格内容
  del name[]

删除后依然可以使用值
  name.pop()

根据值删除内容
  name.remove('')

给列表中排序
  name.sort()
反向排序
  name.sort(reverse=True)
  
临时排序
  sorted(name)
  
反转排序
  name.reverse()

for循环
  for a in b :

生成数字
  range()
  range(起始，结束，增加)

复制列表
  b = a[:]
  
创建元组（不可修改）
  a = ()

字典
  a ={b：c}
  
遍历字典
  for a，b in c.items()

选取字典中的值
  a = ['b': 'c']
  a.keys()指向b
  a.values()指向c

选取不重复的值
  a.set(values())






 
